# DD-NNN: [Feature Name]

**Status:** Draft
**Date:** YYYY-MM-DD
**PB:** [link to PB.md]

## Before state
How it works today. Plain language. Reference real files, flows, or behaviors that exist.

## After state
How it works post-build. Plain language. Same level of concreteness as Before.

## Visual: before → after
Generate two standalone HTML files (`DD-before.html`, `DD-after.html`) showing the workflow as a chart, flowchart, or diagram using inline SVG + CSS. Open in a browser to share with reviewers.

- [`DD-before.html`](./DD-before.html)
- [`DD-after.html`](./DD-after.html)

> **Why HTML, not Excalidraw:** HTML charts are self-contained, render anywhere, version-controlled as plain text, and easier to update than image exports. Use SVG shapes, CSS gradients, and minimal JS if needed.

## Design considerations
The decisions that shaped the After state. For each, name what you picked **and** what you rejected.

- **[Decision 1]:** Picked X over Y because [reason].
- **[Decision 2]:** Picked X over Y because [reason].
- **[Decision 3]:** Picked X over Y because [reason].

## Open questions
Things still being decided. Each should have an owner and a deadline, or be marked "deferred to PRD."

- [ ] [Question] — owner: [name], decide by: [date]

---

> **Next stage:** Once approved, write the PRD (`PRD.md`) — a build checklist, not a spec doc.
